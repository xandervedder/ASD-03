package nl.asd.reservation.domain;

import static org.junit.jupiter.api.Assertions.*;

class TimeslotTest {
    // Business rules:
    // Timeslot was max. 30min?

    // Validatie in een Record is een beetje lastig, not sure hoe we dat gaan doen...
    public void shouldThrowWhenTimeslotIsInvalid() {}

    public void shouldNotThrowWhenTimeslotIsValid() {}
}